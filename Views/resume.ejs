<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Zahin's Resume</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <div class="resume">
      <h1><%= name %>'s Resume</h1>

      <!-- Personal Information -->
      <div class="section">
        <h2>Personal Information</h2>
        <p>Name: <%= name %></p>
        <p>Location: <%= location %></p>
        <p>Occupation: <%= occupation %></p>
      </div>

      <!-- Education -->
      <div class="section">
        <h2>Education</h2>
        <ul>
          <% for (list of education){ %>
          <li><%= list %></li>
          <% } %>
        </ul>
      </div>

      <!-- Work Experience -->
      <div class="section">
        <h2>Work Experience</h2>
        <ul>
          <% for (list of experience){ %>
          <li><%= list %></li>
          <% } %>
        </ul>
      </div>
    </div>
  </body>
</html>
